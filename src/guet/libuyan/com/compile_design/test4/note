1.词法分析
    保留字："const", "var", "procedure", "begin", "end", "if", "then", "call", "while", "do"

    单词类别：
    unknown("未知"), identifier("标识符"), number("数字"),
    plus("加号"), minus("减号"), times("乘号"),division("除号"),
    less("小于"), greater("大于"), comma("逗号"),
    eq("等于号"), neq("不等于号"), leq("小于等于"),
    geq("大于等于"), semicolon("分号"), ifsym("if"),
    thensym("then"), whilesym("while"), dosym("do"),
    callsym("call"), constsym("const"), varsym("var"),
    proceduresym("procedure"), beginsym("begin"), endsym("end"),
    assignment("赋值号"), lparen("左括号"), rparen("右括号"),
    pound("结束符");

2.语法分析
      消除左递归、左公因子，采用LL（1）递归下降分析法

    <主程序> ::= <分程序>
    <分程序> ::= <变量说明部分> <语句>
    <变量说明部分> ::= VAR <标识符 > <更多标识符> ;
    <更多标识符> ::= , <标识符> <更多标识符>
    <更多标识符> ::= <空>
    <语句> ::= <赋值语句>
    <语句> ::= <条件语句>
    <语句> ::= <当型循环语句>
    <语句> ::= <复合语句>
    <语句> ::= <空>
    <赋值语句> ::= <标识符> := <表达式>
    <复合语句> ::= BEGIN <语句> <更多语句> END
    <更多语句> ::= ;<语句> <更多语句>
    <更多语句> ::= <空>
    <条件> ::= <表达式> <关系运算符> <表达式>
    <表达式> ::= + <项> <更多项>
    <表达式> ::= - <项> <更多项>
    <表达式> ::= <项> <更多项>
    <更多项> ::= <加法运算符> <项> <更多项>
    <更多项> ::= <空>
    <项> ::= <因子><更多因子>
    <更多因子> ::= <空>
    <更多因子> ::= <乘法运算符><因子>
    <因子>  ::=  <标识符>
    <因子>  ::=  <无符号整数>
    <因子>  ::=  ‘（’ <表达式> ‘）’
    <加法运算符> ::= + | -
    <乘法运算符> ::= *
    <关系运算符> ::= = | != | < | <= | > | >=
    <条件语句>  ::= IF <条件> THEN <语句>
    <当型循环语句> ::= WHILE <条件> DO <语句>

3.语义分析